import React  from "react";
import { View, Text, StyleSheet } from "react-native";

const ExeciseScreen = () => {
    let message = "This is the Exercise Screen";
    message = "hi there "
    return (
        <Text>{message}</Text>
    )
}

export default ExeciseScreen;

